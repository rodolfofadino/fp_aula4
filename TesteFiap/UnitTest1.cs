using System;
using Xunit;

namespace TesteFiap
{
    public class UnitTest1
    {
        ////[Fact]
        ////public void LoadLastRecipes()
        ////{
        ////    //int totalRecipe = 25;
        ////    int totalRecipe = 20;
        ////    var recipeService = new RecipeService(new Repository());
        ////    var recipes = recipeService.LoadLastRecipes(totalRecipe);
        ////    Assert.Equal(totalRecipe, recipes.Count());
        ////}


        //[Theory]
        //[InlineData(3)]
        //[InlineData(5)]
        ////[InlineData(6)]
        //public void MyFirstTheory(int value)
        //{
        //    Assert.True(IsOdd(value));
        //}
    }
}
